services:
  - type: web
    name: ultima-ventana-demo
    runtime: python
    plan: free
    buildCommand: pip install . -r requirements-web.txt
    startCommand: uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    autoDeploy: true
